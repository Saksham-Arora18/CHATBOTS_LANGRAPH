version: "3.8"

services:
  chatbot:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./faiss_indexes:/app/faiss_indexes
      - ./chatbot.db:/app/data
    env_file:
      - .env
    restart: unless-stopped